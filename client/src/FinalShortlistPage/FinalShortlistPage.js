import React, { useState, useEffect } from "react";

export default function FinalShortlistPage() {
  useEffect(() => {
    window.scrollTo(0, 0);
  }, []);

  return <div>This is the final shortlist page</div>;
}
